/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.sanity.io','images.unsplash.com',' im-3.eefa.co','c7.alamy.com'   ], // Allow Sanity images
      },
    }
      

export default nextConfig;
